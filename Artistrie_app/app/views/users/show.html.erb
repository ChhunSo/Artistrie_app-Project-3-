<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Artistrie</title>
</head>
<body>
  <nav class="navbar navbar-inverse navbar-static-top" role="navigation"> 
      <div class="container">
          <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
          </button>
              <a id="homeLink" class="navbar-brand active" href="/">Artistrie</a>
          </div>

          <div class="collapse navbar-collapse navbar-ex1-collapse">
              <ul class="nav navbar-nav">
                  <li>
                      <a href="/contact">Contact</a>
                  </li>
                  <li>
                      <a href="/competitions">Artist Competitions</a>
                  </li>
              </ul>
              <ul class="nav navbar-nav navbar-right">
                  <li status="active"><a href="/users/<%= @user.id %>"><%= @user.username %>'s Profile</a></li>
                  <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
              </ul>
          </div>
      </div>
  </nav>
<div class='container'>
      <div class="jumbotron">
          <%if @user=current_user %>
             <h2> Welcome <%=@user.username%> </h2>
          <%end%>
      </div>
  <!-- <div class="myApp">
    <div class="PieceShowCtrl">
      <div class='list-group'>
          <a href="/api/pieces/{{piece.id}]" class='list-group-item' ng-repeat='piece in Pieces'>
              <h3> {{piece.title}}</h3>
                <p> {{piece.artist}}</p>
                <p> {{piece.medium}}</p>
          </a>
       </div>
    </div>
  </div>
</div> -->